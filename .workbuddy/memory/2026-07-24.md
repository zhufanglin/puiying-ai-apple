# 2026-07-24 工作日志

## 家校沟通通告系统前端开发

### 完成内容

**阶段一：基础设施建设**
- 安装依赖：react-hook-form, zod, @hookform/resolvers
- 创建通知模块类型定义 `lib/types/notification.ts`（对齐后端 schemas.py）
- 创建 API 服务层 `lib/services/notification.ts`（封装所有 8 个后端 API 端点）
- 创建 Toast 通知系统 `components/ui/ToastProvider.tsx`（Context + 自动消失）
- 创建 Skeleton 骨架屏组件 `components/ui/Skeleton.tsx`（Table/Card/Detail 变体）
- 创建 usePermission Hook `hooks/usePermission.ts`（JWT 解析权限）
- 更新 `app/layout.tsx` 包裹 ToastProvider（通过 ClientLayout wrapper）

**阶段二：路由与页面结构**
- 创建 `notifications/page.tsx`（发送记录列表，重写原有单页面）
- 创建 `notifications/templates/page.tsx`（模板管理，react-hook-form + zod）
- 创建 `notifications/create/page.tsx`（新建通告，含 5 步流程）
- 创建 `notifications/[id]/page.tsx`（通告详情，含日志 + 统计）
- 创建发送确认弹窗 `components/ui/SendConfirmDialog.tsx`
- 所有页面配备 loading.tsx（Skeleton）+ error.tsx（重试按钮）

**阶段三：类型检查**
- tsc --noEmit 通过，零错误

### 关键技术决策
- 后端 API 已完整实现，前端直接调用真实 API（非 mock）
- 侧边栏已有"通告管理"入口，无需修改 layout
- 批次发送控制按钮已预留（置灰 + TODO 注释）
- PDF 导出功能已实现调用，如端点未就绪会友好提示

### 代码统计
- 新建文件：19 个
- 修改文件：3 个（app/layout.tsx, app/core/security.py, app/modules/accounts/router.py）
- 备份文件：1 个（page.tsx.bak）

### bcrypt 兼容性修复
- passlib 1.7.4 与 bcrypt 5.x 不兼容，改为直接使用 bcrypt 库
- 修改 `app/core/security.py`：`hash_password` / `verify_password` 改用 `bcrypt` 模块
- bcrypt 降级到 4.3.0

### JWT 权限修复
- `create_access_token` 增加 `permissions` 参数写入 JWT
- `accounts/router.py` 登录时从 DB 加载权限码传入 token
- 前端 `usePermission` 增加 `role === "super_admin"` 兜底判断

### 发送通知修复
- `notifications/service.py` 改为直接查 SQLAlchemy Student 模型（原实现使用文件存储 StudentService，数据源不一致）

### 端到端测试结果
- API 测试: 20/20 通过 ✅
- 前端页面: 3/3 200 OK ✅
- 现有功能回归: 5/5 正常 ✅
- 发送通知: 9 条家长手机号找到，mock 模式发送成功

### 运行环境
- 前端: http://127.0.0.1:4000
- 后端: http://127.0.0.1:8002
- 数据库: SQLite (apps/api/app.db)
- 临时文件（提交前删除）: .env, app.db
